import ts from 'typescript';

/**
 * Walk a typescript program, using specified entry points, returning
 * JSON information about the code
 */
export function walkProgram(project: ts.Program, entries: string[]) {}
